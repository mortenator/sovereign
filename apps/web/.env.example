# OnlyOffice Document Server URL
# Default: http://localhost:8080 (local Docker instance)
VITE_OO_SERVER_URL=http://localhost:8080

# Application base URL used when building absolute callback URLs for OO DS
# Default: http://localhost:5173
VITE_APP_URL=http://localhost:5173

# ---------------------------------------------------------------------------
# JWT TOKEN (PRODUCTION ONLY)
# ---------------------------------------------------------------------------
# OnlyOffice Document Server should be configured with a JWT secret in
# production to prevent unauthorized document access. When JWT is enabled on
# the DS, every document config object must include a signed token.
#
# Generate tokens server-side (Node.js example):
#   const jwt = require('jsonwebtoken')
#   const token = jwt.sign(config, process.env.OO_JWT_SECRET)
#
# NEVER disable JWT validation in production. For local development you can
# run the DS with JWT disabled (the default Docker image ships with it off).
#
# Do NOT add a raw JWT secret here — it belongs in a server-side environment,
# not in a Vite frontend bundle where it would be exposed to browsers.
# OO_JWT_SECRET=your-secret-here  ← server-side only, never VITE_* prefixed
